# ToDo Listen Manager (MVP (Minimum Viable Product))

## DockerImage bauen
docker build -t geteyes/my-todo-app .

## Dockerimage starten
docker run --rm --name mytodo -d -p 3000:3000 geteyes/my-todo-app
